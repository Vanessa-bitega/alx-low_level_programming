#include "main.h"
/**
 * add - function that adds up two intergers and return the result
 * @a: first number to add
 * @b: second number to add
 * Return: result
 */
int add(int a, int b)
{
	return (a + b);
}

