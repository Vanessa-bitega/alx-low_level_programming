#include "main.h"
#include<stdio.h>
/**
 * main - Entry point
 * @str: holds the quote
 * Return: Always 0 (Success)
 */
void _puts(char *str);
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
